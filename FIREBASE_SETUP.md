# دليل إعداد Firebase - خطوة بخطوة 🔥

هذا الدليل يشرح بالتفصيل كيفية إعداد Firebase للتطبيق.

## الخطوة 1: إنشاء حساب Firebase

1. اذهب إلى [Firebase Console](https://console.firebase.google.com/)
2. سجل الدخول بحساب Google الخاص بك
3. اضغط على "Add project" أو "إضافة مشروع"

## الخطوة 2: إنشاء المشروع

1. أدخل اسم المشروع: `Donation Storage` (أو أي اسم تريده)
2. اضغط Continue
3. اختر تفعيل Google Analytics (اختياري)
4. اضغط Create Project
5. انتظر حتى ينتهي إنشاء المشروع
6. اضغط Continue

## الخطوة 3: تفعيل Authentication

1. من القائمة الجانبية، اختر **Authentication**
2. اضغط "Get Started"
3. اختر "Sign-in method" من الأعلى
4. اضغط على "Google"
5. فعّل الخيار (Enable)
6. اختر Support email (بريدك الإلكتروني)
7. اضغط Save

## الخطوة 4: إنشاء قاعدة البيانات Firestore

1. من القائمة الجانبية، اختر **Firestore Database**
2. اضغط "Create database"
3. اختر موقع الخادم:
   - اختر `eur3 (europe-west)` لأقرب خادم لمصر
4. اختر "Start in test mode" للبداية
5. اضغط Enable
6. انتظر حتى يتم إنشاء قاعدة البيانات

### إعداد قواعد الأمان (Security Rules):

1. اذهب إلى تبويب **Rules**
2. استبدل المحتوى بالكود التالي:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // يسمح فقط للمستخدمين المسجلين بالقراءة والكتابة
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```

3. اضغط **Publish**

## الخطوة 5: إضافة Android App

### 5.1 إضافة التطبيق:

1. من **Project Overview**، اضغط على أيقونة Android (رمز الروبوت الأخضر)
2. في حقل "Android package name"، أدخل:
   ```
   com.bloodcenter.donation_storage
   ```
3. (اختياري) App nickname: `Donation Storage`
4. اضغط **Register app**

### 5.2 تحميل google-services.json:

1. اضغط "Download google-services.json"
2. احفظ الملف على جهازك
3. **مهم جداً**: انقل الملف إلى المشد التالي في مشروعك:
   ```
   c:\Users\zidan\Desktop\Donation Storage\android\app\google-services.json
   ```

### 5.3 إضافة SHA-1 Fingerprint (مطلوب لـ Google Sign-In):

#### للتطوير (Debug):

1. افتح Command Prompt
2. انتقل إلى مجلد التطبيق:
   ```bash
   cd "c:\Users\zidan\Desktop\Donation Storage"
   ```

3. نفذ الأمر التالي للحصول على SHA-1:
   ```bash
   cd android
   gradlew signingReport
   ```

4. ابحث عن السطر الذي يبدأ بـ `SHA1:` تحت `Variant: debug`
5. انسخ القيمة (مثال: `A1:B2:C3:...`)

6. ارجع إلى Firebase Console
7. اذهب إلى Project Settings (الإعدادات)
8. تحت "Your apps"، اختر التطبيق Android
9. اضغط "Add fingerprint"
10. الصق SHA-1
11. اضغط Save

## الخطوة 6: إنشاء المجموعات (Collections) الأولية

Firestore سينشئ المجموعات تلقائياً عند أول عملية كتابة، لكن يمكنك إنشاءها يدوياً:

1. اذهب إلى **Firestore Database**
2. اضغط "Start collection"
3. أدخل Collection ID: `categories`
4. اضغط Next
5. أضف مستند تجريبي:
   - Document ID: (اتركه auto-ID)
   - Fields:
     - `name` (string): `مثال`
     - `unit` (string): `كرتونة`
     - `createdAt` (timestamp): (اضغط على الساعة لاختيار الوقت الحالي)
6. اضغط Save

كرر نفس العملية للمجموعات:
- `items`
- `transactions`

(أو يمكنك تركهم لينشئوا تلقائياً عند استخدام التطبيق)

## الخطوة 7: التحقق من الإعداد

### تفقد القائمة:

- ✅ تم إنشاء مشروع Firebase
- ✅ تم تفعيل Google Authentication
- ✅ تم إنشاء Firestore Database
- ✅ تم تحميل google-services.json ووضعه في المكان الصحيح
- ✅ تم إضافة SHA-1 fingerprint
- ✅ تم ضبط Security Rules

## استكشاف مشاكل شائعة

### Problem: "API not enabled"
**الحل:**
1. اذهب إلى [Google Cloud Console](https://console.cloud.google.com/)
2. اختر مشروعك
3. فعّل هذه الخدمات:
   - Google Sign-In API
   - Cloud Firestore API

### Problem: Google Sign-In لا يعمل
**الحل:**
- تأكد من إضافة SHA-1 fingerprint
- تأكد من تفعيل Google في Authentication
- تأكد من اختيار Support email

### Problem: لا يمكن الكتابة في Firestore
**الحل:**
- تأكد من قواعد الأمان (Security Rules)
- تأكد من تسجيل الدخول بنجاح

## روابط مفيدة

- [Firebase Console](https://console.firebase.google.com/)
- [Flutter Firebase Setup Guide](https://firebase.flutter.dev/docs/overview)
- [Firestore Documentation](https://firebase.google.com/docs/firestore)

## ملاحظات مهمة

1. **البيانات في Test Mode**: إذا اخترت "test mode"، فإن قواعد الأمان ستنتهي بعد 30 يوماً. تأكد من تحديثها للقواعد في الخطوة 4.

2. **الأمان**: لا تشارك ملف `google-services.json` مع أي شخص.

3. **النسخ الاحتياطي**: Firebase يحفظ البيانات تلقائياً، لكن يُنصح بعمل export دوري للبيانات المهمة.

---

**بعد إتمام جميع الخطوات، يمكنك تشغيل التطبيق!** 🎉
